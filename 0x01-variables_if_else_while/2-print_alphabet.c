#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char div;

	for (div = 'a'; div <= 'z'; fav++)
	{
		putchar(div);
	}
	putchar('\n');
	return (0);
}
