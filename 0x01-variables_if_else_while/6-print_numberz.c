#include <stdio.h>

/**
 * main - Entry point
 * author: favvy
 * Return: Always 0(Success)
 */
int main(void)
{
	int data;

	for (data = 48; data < 58; data++)
		putchar(data);
	putchar('\n');
	return (0);
}
