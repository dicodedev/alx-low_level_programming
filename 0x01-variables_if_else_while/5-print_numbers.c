#include <stdio.h>

/**
 * main - Entry point
 * author: favvy
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int data;

	for (data = 0; data < 10; data++)
	{
		printf("%d", data);
	}
	printf("\n");
	return (0);
}
